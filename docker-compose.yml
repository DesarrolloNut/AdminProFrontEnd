version: '3.8'

services:
  nutriciosa-admin-frontend:
    build: .
    container_name: nutriciosa-admin-frontend
    ports:
      - "80:80"
    environment:
      - API_URL=${API_URL}
    restart: unless-stopped
